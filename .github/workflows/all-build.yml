name: Multi-Platform Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            artifact_name: FileOrganizer-Windows.exe
            backend_ext: pyd
          - os: ubuntu-latest
            artifact_name: FileOrganizer-Linux
            backend_ext: so
          - os: macos-latest
            artifact_name: FileOrganizer-Mac
            backend_ext: so

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pybind11 pyqt6 pyinstaller pillow

    - name: Compile C++ Backend (Windows)
      if: runner.os == 'Windows'
      run: |
        $PYTHON_INCLUDE = python -c "from sysconfig import get_paths; print(get_paths()['include'])"
        $PYBIND11_INCLUDE = "extern/pybind11/include"
        $PythonVersion = python -c "import sys; print(f'{sys.version_info.major}{sys.version_info.minor}')"
        $PYTHON_LIBDIR = python -c "import sys; import pathlib; print(pathlib.Path(sys.exec_prefix) / 'libs')"
        $PYTHON_LIB = Join-Path $PYTHON_LIBDIR "python$PythonVersion.lib"
        if (-not (Test-Path $PYTHON_LIB)) {
          Write-Host "Python .lib not found: $PYTHON_LIB"
          Exit 1
        }
        $vs2022Paths = @(
          "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat",
          "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat",
          "C:\Program Files\Microsoft Visual Studio\2022\Professional\VC\Auxiliary\Build\vcvars64.bat"
        )
        $vcvars = $vs2022Paths | Where-Object { Test-Path $_ } | Select-Object -First 1
        if (-not $vcvars) {
          Write-Host "vcvars64.bat not found in any expected VS2022 installation location."
          Exit 1
        }
        $CLFLAGS = "/LD /EHsc /std:c++17 /I$PYTHON_INCLUDE /I$PYBIND11_INCLUDE sortAlgo.cpp /Fe:file_sorter.pyd $PYTHON_LIB"
        cmd /c "`"$vcvars`" && cl $CLFLAGS"
      shell: pwsh

    - name: Compile C++ Backend (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        PYTHON_INC=$(python -c "from sysconfig import get_paths; print(get_paths()['include'])")
        PYBIND_INC="extern/pybind11/include"
        FLAGS="-O3 -Wall -shared -std=c++17 -fPIC -I$PYBIND_INC -I$PYTHON_INC"
        if [ "$RUNNER_OS" == "macOS" ]; then
          FLAGS="$FLAGS -undefined dynamic_lookup"
        fi
        g++ $FLAGS sortAlgo.cpp -o file_sorter.${{ matrix.backend_ext }}
        chmod +x file_sorter.${{ matrix.backend_ext }}
      shell: bash

    - name: Build with PyInstaller
      run: |
        if [ "$RUNNER_OS" == "Windows" ]; then
          pyinstaller --noconsole --onefile --icon=lain.ico --add-data "file_sorter.pyd;." --add-data "lain.ico;." fileSYSman.py
        else
          pyinstaller --noconsole --onefile --icon=lain.ico --add-data "file_sorter.so:." --add-data "lain.ico:." fileSYSman.py
        fi
      shell: bash

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.os }}-build
        path: dist/fileSYSman* # Captures the executable regardless of extension
